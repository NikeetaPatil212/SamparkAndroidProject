<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Checked (ON) state -->
    <item android:color="@color/green_dark" android:state_checked="true"/>
    <!-- Unchecked (OFF) state -->
    <item android:color="@color/bg_light" android:state_checked="false"/>
</selector>
