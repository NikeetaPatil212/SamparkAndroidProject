<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

   <!-- <style name="Theme.AndroidProject" parent="Base.Theme.AndroidProject" />-->


    <style name="Theme.AndroidProject" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/hero_green</item>
        <item name="colorPrimaryDark">@color/hero_green</item>
        <item name="android:statusBarColor">@color/hero_green</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.Splash" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/bg_splash_gradient</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>


 <!--   <style name="Theme.Sampark" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>-->


    <style name="Theme.Sampark" parent="Theme.MaterialComponents.Light.NoActionBar">

        <!-- Disable auto dark -->
        <item name="android:forceDarkAllowed">false</item>

        <!-- Brand colors -->
        <item name="colorPrimary">@color/green_primary</item>
        <item name="colorPrimaryVariant">@color/green_primary</item>
        <item name="colorOnPrimary">@color/card_white</item>

        <!-- Surface & background -->
        <item name="colorSurface">@color/card_white</item>
        <item name="colorOnSurface">@color/text_dark</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/card_white</item>
        <item name="android:windowLightStatusBar">true</item>

       <!-- <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>-->
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.Sampark" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

</resources>